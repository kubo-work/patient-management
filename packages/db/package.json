{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "node --env-file=../api/.env scripts/guarded-migrate.mjs dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "node --env-file=../api/.env ../../node_modules/.bin/prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  }
}
